import styled from '@emotion/styled';
import { AppTitle } from 'components/atoms/AppTitle';

export const Header = () => {
    return (
        <div>
            <AppTitle label="test"/>
        </div>
    );
};

